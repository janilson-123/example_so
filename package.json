{
	"name": "test",
	"private": true,
	"workspaces": [
		"front",
		"back",
		"shared"
	],
	"scripts": {
		"ts-back": "yarn workspace @test/back ts",
		"build-back": "yarn workspace @test/back build",
		"build-shared": "yarn workspace @test/shared build",
		"test-back": "yarn workspace @test/back run-test"
	},
	"devDependencies": {
		"typescript": "^4.6.3"
	}
}
